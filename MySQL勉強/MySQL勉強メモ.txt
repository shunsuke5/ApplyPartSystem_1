MySQL勉強メモ

・クエリーは通常、SQLステートメントの後にセミコロンが続くもので構成される。
　クエリーを発行すると、mysqlはそのクエリーをサーバーに送信して実行し、
　結果を表示してから別のクエリーの準備ができていることを示す別のmysqlプロンプトを出力する。

・MySQLは、クエリーの出力を表形式(行と列)で表示する。最初の行には列のラベルが表示される。

・セミコロンでステートメントの終了位置が決定される。